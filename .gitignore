# OS / editor cruft
.DS_Store
Thumbs.db
*~
*.swp

# Future firmware build output (Phase 0.5+)
build/
.pio/
*.bin
*.elf

# Overnight soak captures (script itself is tracked)
firmware/soak/*.log


# Private working material — never published (history purged 2026-09-05)
media/
PLAN.md
